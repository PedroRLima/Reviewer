<div class="panel container">
	<h2>Log in</h2>
		<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
		  <div class="form-inputs">
		  	<div class="control-group">
		    <%= f.input :email,label:"Matrícula", required: false, autofocus: true %>
		    </div>

		   	<div class="control-group">
		    <%= f.input :password,label:"Senha", required: false %>
		    </div>

		    <div class="control-group">
		    <%= f.input :remember_me,label:"Lembrar" ,as: :boolean if devise_mapping.rememberable? %>
		    </div>

		  <div class="form-actions">
		    <%= f.button :submit, "Log in", class: " controls btn" %>
		  </div>

		<% end %>
	<%= render "devise/shared/links" %>
	</div>
</div>

